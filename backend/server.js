/**
 * @deprecated Use the TypeScript backend instead:
 *   cd backend && npm install && npm run dev
 * Serves on http://localhost:4000 (not 3003).
 */
console.error(
  'backend/server.js is deprecated. Run: cd backend && npm run dev'
);
process.exit(1);
